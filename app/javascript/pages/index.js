import React from 'react'
import ReactDOM from 'react-dom'
import DecisionsContainer from '../decisions/components/DecisionsContainer';

const container = document.querySelector('#decisions');
// const data = JSON.parse(container.getAttribute('data'));
// ReactDOM.render(<DecisionsContainer decisions={data} />, container);
